#pragma once

#include "CoreMinimal.h"

/**
 * Console commands for performance testing and optimization
 * These commands are automatically registered when the module loads
 */
namespace PerformanceConsoleCommands
{
    // Console commands are defined in the .cpp file using static FAutoConsoleCommand
    // This header exists for organization and potential future expansion
}